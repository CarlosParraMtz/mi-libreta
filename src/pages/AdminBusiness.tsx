import { ArrowLeft, Check, CreditCard, ExternalLink, Save, ShieldAlert, Users } from 'lucide-react'
import { arrayUnion, doc, setDoc } from 'firebase/firestore'
import { useEffect, useState, type FormEvent } from 'react'
import { Link, useNavigate, useParams } from 'react-router-dom'
import { authInputClass } from '../components/AuthLayout'
import { apiRequest } from '../lib/api'
import { auth, db } from '../lib/firebase'
import { moduleOptions } from '../lib/modules'
import type { LedgerData, ProductModule } from '../lib/types'

export function AdminBusiness() {
  const { id = '' } = useParams()
  const navigate = useNavigate()
  const [business, setBusiness] = useState<LedgerData | null>(null)
  const [modules, setModules] = useState<ProductModule[]>([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState('')
  const load = () => { setLoading(true); void apiRequest<{ business: LedgerData }>(`/admin/businesses/${id}`).then((result) => { setBusiness(result.business); setModules(result.business.enabledModules || []) }).catch((issue) => setError(issue instanceof Error ? issue.message : 'No pudimos cargar la libreta.')).finally(() => setLoading(false)) }
  useEffect(() => {
    let active = true
    void apiRequest<{ business: LedgerData }>(`/admin/businesses/${id}`)
      .then((result) => { if (active) { setBusiness(result.business); setModules(result.business.enabledModules || []) } })
      .catch((issue) => { if (active) setError(issue instanceof Error ? issue.message : 'No pudimos cargar la libreta.') })
      .finally(() => { if (active) setLoading(false) })
    return () => { active = false }
  }, [id])
  const save = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); setLoading(true); try { await apiRequest(`/admin/businesses/${id}`, { method: 'PATCH', body: JSON.stringify({ businessName: form.get('businessName'), ownerName: form.get('ownerName'), businessType: form.get('businessType'), phone: form.get('phone'), enabledModules: modules }) }); await load() } catch (issue) { setError(issue instanceof Error ? issue.message : 'No pudimos guardar.') } finally { setLoading(false) } }
  const subscriptionAction = async (action: string) => { setLoading(true); setError(''); try { await apiRequest(`/admin/businesses/${id}/subscription`, { method: 'POST', body: JSON.stringify({ action }) }); await load() } catch (issue) { setError(issue instanceof Error ? issue.message : 'No pudimos cambiar la suscripción.'); setLoading(false) } }
  const openAsSupport = async () => {
    const user = auth?.currentUser
    if (!user || !db) return
    setLoading(true); setError('')
    try {
      await setDoc(doc(db, 'users', user.uid), { defaultBusinessId: id, businessIds: arrayUnion(id), updatedAt: new Date().toISOString() }, { merge: true })
      navigate('/dashboard')
    } catch (issue) { setError(issue instanceof Error ? issue.message : 'No pudimos abrir la libreta.'); setLoading(false) }
  }
  if (!business) return <main className="grid min-h-screen place-items-center bg-cream font-black text-ink/40">{error || 'Cargando libreta…'}</main>
  const subscription = business.subscription
  return <main className="min-h-screen bg-cream px-4 py-6 text-ink sm:px-8 lg:px-12"><div className="mx-auto max-w-6xl"><Link to="/admin" className="inline-flex items-center gap-2 text-sm font-black text-ink/45"><ArrowLeft size={18} /> Volver a libretas</Link><header className="mt-5 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-black uppercase tracking-[.16em] text-coral">Administrar libreta</p><h1 className="mt-2 text-4xl font-black tracking-tight">{business.businessName}</h1><p className="mt-1 font-semibold text-ink/40">ID: {id}</p></div><button onClick={openAsSupport} disabled={loading} className="flex items-center justify-center gap-2 rounded-2xl bg-coral px-5 py-3.5 font-black text-white shadow-soft disabled:opacity-50"><ExternalLink size={18} /> Abrir como soporte</button></header>{error && <p className="mt-5 rounded-2xl bg-peach/55 p-4 font-bold">{error}</p>}<div className="mt-6 grid gap-5 lg:grid-cols-[1.2fr_.8fr]"><form onSubmit={save} className="rounded-[2rem] bg-white p-6 shadow-soft"><h2 className="text-xl font-black">Configuración del negocio</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><label><span className="mb-1.5 block text-sm font-bold text-ink/55">Negocio</span><input name="businessName" defaultValue={business.businessName} className={authInputClass} /></label><label><span className="mb-1.5 block text-sm font-bold text-ink/55">Encargado</span><input name="ownerName" defaultValue={business.ownerName} className={authInputClass} /></label><label><span className="mb-1.5 block text-sm font-bold text-ink/55">Giro</span><input name="businessType" defaultValue={business.businessType} className={authInputClass} /></label><label><span className="mb-1.5 block text-sm font-bold text-ink/55">WhatsApp</span><input name="phone" defaultValue={business.phone} className={authInputClass} /></label></div><p className="mt-6 text-sm font-black">Módulos activos</p><div className="mt-3 grid grid-cols-2 gap-3">{moduleOptions.map(({ id: moduleId, title, color, icon: Icon }) => { const selected = modules.includes(moduleId); return <button type="button" key={moduleId} onClick={() => setModules((items) => selected ? items.filter((item) => item !== moduleId) : [...items, moduleId])} className={`relative rounded-2xl border-2 p-4 text-left ${selected ? 'border-ink' : 'border-transparent bg-cream opacity-50'}`}><span className={`grid size-9 place-items-center rounded-xl ${color}`}><Icon size={17} /></span>{selected && <Check size={16} className="absolute right-3 top-3" />}<p className="mt-3 font-black">{title}</p></button> })}</div><button disabled={loading} className="mt-6 flex w-full items-center justify-center gap-2 rounded-2xl bg-ink px-5 py-3.5 font-black text-white disabled:opacity-50"><Save size={18} /> Guardar configuración</button></form><div className="space-y-5"><section className="rounded-[2rem] bg-ink p-6 text-white shadow-card"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-2xl bg-lime text-ink"><CreditCard size={20} /></span><div><p className="text-xs font-bold text-white/45">Suscripción</p><h2 className="text-xl font-black">{subscription.status}</h2></div></div><p className="mt-4 text-sm font-semibold text-white/45">{subscription.stripeSubscriptionId ? `Stripe: ${subscription.stripeSubscriptionId}` : 'Aún no hay una suscripción de Stripe.'}</p><div className="mt-5 grid grid-cols-2 gap-2"><button onClick={() => subscriptionAction(subscription.accessOverride === 'suspended' ? 'restore' : 'suspend')} className="rounded-xl bg-white/10 px-3 py-2.5 text-sm font-black">{subscription.accessOverride === 'suspended' ? 'Restaurar acceso' : 'Suspender acceso'}</button>{subscription.stripeSubscriptionId && <button onClick={() => subscriptionAction(subscription.cancelAtPeriodEnd ? 'resume' : 'cancel')} className="rounded-xl bg-coral px-3 py-2.5 text-sm font-black">{subscription.cancelAtPeriodEnd ? 'Reanudar cobro' : 'Cancelar al final'}</button>}</div></section><section className="rounded-[2rem] bg-white p-6 shadow-soft"><div className="flex items-center gap-3"><Users size={21} /><h2 className="text-xl font-black">Contenido</h2></div><div className="mt-5 grid grid-cols-2 gap-3">{[['Clientes', business.customers.length], ['Pedidos', business.orders.length], ['Fiados', business.credits.length], ['Apartados', business.layaways.length]].map(([label, value]) => <div key={label} className="rounded-2xl bg-cream p-4"><p className="text-2xl font-black">{value}</p><p className="text-xs font-bold text-ink/40">{label}</p></div>)}</div></section></div></div><section className="mt-5 rounded-[2rem] bg-white p-6 shadow-soft"><div className="flex items-center gap-2"><ShieldAlert size={20} /><h2 className="text-xl font-black">Actividad reciente</h2></div><div className="mt-5 grid gap-5 lg:grid-cols-2"><div><p className="text-sm font-black">Clientes</p><div className="mt-2 divide-y divide-ink/8">{business.customers.slice(0, 5).map((customer) => <div key={customer.id} className="flex justify-between py-3 text-sm"><span className="font-bold">{customer.name}</span><span className="text-ink/40">{customer.phone || 'Sin teléfono'}</span></div>)}</div></div><div><p className="text-sm font-black">Pedidos</p><div className="mt-2 divide-y divide-ink/8">{business.orders.slice(0, 5).map((order) => <div key={order.id} className="flex justify-between gap-3 py-3 text-sm"><span className="truncate font-bold">{order.items}</span><span className="shrink-0 text-ink/40">{order.status}</span></div>)}</div></div></div></section></div></main>
}
